
:queues:
  - default
  - active_storage_analysis

:scheduler:
  :dynamic: true
  :schedule:
    purge_orphaned_files:
      cron: "0 0 * * *"
      # Use ActiveJob class (compatible with both Sidekiq and Solid Queue)
      class: PurgeOrphanedUploadsJob