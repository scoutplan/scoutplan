local.scoutplan.org {
      reverse_proxy localhost:3000
      # tls /root/certs/local.scoutplan.org.pem /root/certs/local.scoutplan.org-key.pem
      tls ./certs/local.scoutplan.org.pem ./certs/local.scoutplan.org-key.pem
}
