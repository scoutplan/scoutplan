li(id="#{dom_id(member)}")
  = link_to member.display_name, new_unit_event_rsvp_path(@event.unit, @event, unit_membership_id: member.id),
            class: "block text-blue-600 hover:text-blue-800 underline py-1",
            id: dom_id(member),
            draggable: true,
            data: { turbo_frame: "modal", action: "dragstart->dashboard-drop#dragstart drag->dashboard-drop#drag" }