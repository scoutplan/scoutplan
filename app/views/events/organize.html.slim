header.mb-4
  h1 = @event.title

#responses
  = render 'events/partials/organize/responses'

scss:
  .actions {
    display: none;
    margin-left: 1rem;
  }

  tr {
    cursor: pointer;
    &:hover {
      .actions {
        display: inline-block;
      }
    }
  }

  .headcount {
    color: #aaa;
  }
