- return unless event.requires_rsvp && policy(event).organize?

= link_to unit_event_rsvps_path(@unit, event),
          class: "inline-block px-4 py-2 font-bold rounded text-cyan-600 border border-cyan-600 text-sm hover:text-cyan-800 hover:border-cyan-800",
          title: "Organize this event",
          id: "organize_#{dom_id(event)}_link",
          data: { turbo_action: "advance", turbo_frame: "main" }
  = "View and organize RSVPs"