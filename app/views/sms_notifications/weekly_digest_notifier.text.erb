Hi, <%= @recipient.first_name %>. This week at <%= current_unit.name %>:

<% current_unit.events.published.this_week.each do |event| %>
* <%= event.title %> on <%= event.starts_at.in_time_zone.strftime('%A') %>
<% end %>

Visit <%= magic_url_for(@recipient, unit_events_url(current_unit)) %> for the full schedule.
