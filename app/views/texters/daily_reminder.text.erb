<% date_format = '%I:%M %p' %>

Hi, <%= @member.first_name %>. Today at <%= @member.unit.name %>:


<% @events.each do |event| %>
  <%= "* #{event.title} at #{event.starts_at.strftime(date_format)}\n" %>
<% end %>

See the full calendar at <%= unit_events_url(@member.unit, r: @magic_link_token) %>.