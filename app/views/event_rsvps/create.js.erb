// render the partial
var responses = document.querySelector('#responses');
responses.innerHTML = "<%= j render partial: 'events/partials/organize/responses' %>";

// show a toast
var toastBody = document.querySelector('#toast_body');
toastBody.innerHTML = '<%= "#{ t('events.organize.update_toast', user_name: @member.display_first_name, event_title: @event.title)}" %>';
toast.show();

setupHoverLinks();
