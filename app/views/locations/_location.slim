div.p-4.hover:bg-stone-100
  = link_to edit_unit_location_path(@unit, location),
            data: { turbo_action: "advance" } do

    = content_tag :div, location.name || location.map_name || "No name", class: "font-bold"
    = content_tag :div, location.address, class: "font-bold"
    - if location.phone.present?
      = content_tag :div, class: "font-bold" do
        = location.phone

  - if location.website.present?
    = content_tag :div do
      = link_to location.website, location.website, class: "text-sky-500 external-link font-bold", target: "_new"