<% if @events.count == 1 %>
<% event = @events.first %>
Hi, <%= @member.display_first_name %>. As a reminder, <%= event.title %> is <%= deictic_string_for_time_interval_from_day(event.starts_at) %> at <%= event.starts_at.in_time_zone.strftime("%-I:%M %p") %> at <%= event.full_address %>.
<% else %>
Hi, <%= @member.display_first_name %>. Today at <%= @member.unit.name %>:

<% @events.each do |event| %>
<%= "* #{event.title} at #{event.starts_at.in_time_zone.strftime('%-I:%M %p')}\n" %> at <%= event.full_address %>.
<% end %>
<% end %>

See the full <%= @member.unit.name %> calendar at <%= url_without_scheme(magic_url_for(@member, unit_events_path(@member.unit))) %>.