<% if @events.count == 1 %>
<% event = @events.first %>
Hi, <%= @member.display_first_name %>. As a reminder, the <%= event.title %> is <%= deictic_day(event.starts_at) %> at <%= event.starts_at.strftime("%I:%M %p") %>.
<% else %>
Hi, <%= @member.display_first_name %>. Today at <%= @member.unit.name %>:

<% @events.each do |event| %>
<%= "* #{event.title} at #{event.starts_at.strftime('%I:%M %p')}\n" %>
<% end %>
<% end %>

See the full <%= @member.unit.name %> calendar at <%= url_without_scheme(magic_url_for(@member, unit_events_path(@member.unit))) %>.