Hi, <%= @member.display_first_name %>. Here's what's going on at <%= @member.unit.name %>:

<% @events.each do |event| %>
<%= "* #{event.title} on #{event.starts_at.strftime('%A')}" %>

<% end %>
Visit <%= unit_events_url(@member.unit, r: @magic_link_token) %> to view the full calendar.