Hi, <%= @member.display_first_name %>. Coming up at <%= @member.unit.name %>:

<% @events.each do |event| %>
<%= "* #{event.title} on #{event.starts_at.in_time_zone.strftime('%A')}#{@open_rsvps.include?(event) ? ' (RSVP needed)' : ''}" %>
<% end %>

<%- if (unresponded_count = @service.unresponded_events.count).positive? %><%= I18n.t("unanswered_rsvps", count: unresponded_count) %>. <% end %>Visit <%= magic_url_for(@member, unit_path(@member.unit)) %> to view the full schedule and RSVPs.