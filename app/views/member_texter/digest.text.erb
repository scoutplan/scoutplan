Hi, <%= @member.display_first_name %>. Coming up at <%= @member.unit.name %>:

<% @events.each do |event| %>
<%= "* #{event.title} on #{event.starts_at.in_time_zone.strftime('%A')}" %>

<% end %>
Visit <%= url_without_scheme(magic_url_for(@member, unit_events_path(@member.unit))) %> to view the full calendar.